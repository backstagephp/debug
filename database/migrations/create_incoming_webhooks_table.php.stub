<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('incoming_webhooks', function (Blueprint $table): void {
            $table->id();
            // The party that delivered it, taken from the path: "rinkel",
            // "productive", "mails".
            $table->string('source')->index();
            $table->string('method');
            $table->text('path');
            $table->text('url');
            $table->string('ip')->nullable();
            $table->json('headers')->nullable();
            $table->json('query')->nullable();
            $table->longText('payload')->nullable();
            $table->unsignedBigInteger('body_size')->default(0);
            $table->unsignedSmallInteger('status')->index();
            $table->longText('response_body')->nullable();
            $table->unsignedBigInteger('response_size')->default(0);
            $table->unsignedInteger('duration_ms')->nullable();
            $table->timestamp('received_at')->index();
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('incoming_webhooks');
    }
};
